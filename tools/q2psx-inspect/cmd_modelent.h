/*
 * cmd_modelent.h — the effect models a MODEL ENTITY can bind, censused across
 * every CastList on the disc, and the think's clock run against each of them.
 */
#ifndef Q2PSX_CMD_MODELENT_H
#define Q2PSX_CMD_MODELENT_H

#include "disc.h"

int cmd_modelent(const disc *d);

#endif /* Q2PSX_CMD_MODELENT_H */
